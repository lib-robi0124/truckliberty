@using Vozila.Domain.Enums
@model AdminOrderDashboardVM
@{
    ViewData["Title"] = "Order Dashboard";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <!-- Page Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0 text-gray-800">
            <i class="fas fa-truck mr-2"></i>Order Dashboard
        </h1>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus-circle mr-2"></i>Create New Order
        </a>
    </div>

    <!-- Statistics Cards -->
    <div class="row mb-4">
        <!-- Total Orders -->
        <div class="col-xl-2 col-md-4 mb-4">
            <div class="card border-left-primary shadow h-100 py-2">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                                Total Orders
                            </div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@Model.TotalOrders</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-clipboard-list fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Pending Orders -->
        <div class="col-xl-2 col-md-4 mb-4">
            <div class="card border-left-warning shadow h-100 py-2">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">
                                Pending
                            </div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@Model.PendingOrders</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-clock fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- In Progress -->
        <div class="col-xl-2 col-md-4 mb-4">
            <div class="card border-left-info shadow h-100 py-2">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-info text-uppercase mb-1">
                                In Progress
                            </div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@Model.PendingOrders</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-truck-loading fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Completed -->
        <div class="col-xl-2 col-md-4 mb-4">
            <div class="card border-left-success shadow h-100 py-2">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-success text-uppercase mb-1">
                                Completed
                            </div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@Model.FinishedOrders</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-check-circle fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Cancelled -->
        <div class="col-xl-2 col-md-4 mb-4">
            <div class="card border-left-danger shadow h-100 py-2">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-danger text-uppercase mb-1">
                                Cancelled
                            </div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@Model.CancelledOrders</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-ban fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Quick Actions -->
        <div class="col-xl-2 col-md-4 mb-4">
            <div class="card border-left-secondary shadow h-100 py-2">
                <div class="card-body text-center">
                    <div class="text-xs font-weight-bold text-secondary text-uppercase mb-2">
                        Quick Actions
                    </div>
                    <div class="btn-group-vertical w-100">
                        <a asp-action="ManageOrder" class="btn btn-sm btn-outline-primary mb-1">
                            <i class="fas fa-list mr-1"></i>View All Orders
                        </a>
                        <a asp-action="Create" class="btn btn-sm btn-outline-success mb-1">
                            <i class="fas fa-plus mr-1"></i>New Order
                        </a>
                        <a href="#" class="btn btn-sm btn-outline-info mb-1" data-toggle="modal" data-target="#searchModal">
                            <i class="fas fa-search mr-1"></i>Search Orders
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Recent Orders Table -->
    <div class="card shadow mb-4">
        <div class="card-header py-3 d-flex justify-content-between align-items-center">
            <h6 class="m-0 font-weight-bold text-primary">
                <i class="fas fa-history mr-2"></i>Recent Orders
            </h6>
            <a asp-action="ManageOrder" class="btn btn-sm btn-primary">
                View All <i class="fas fa-arrow-right ml-1"></i>
            </a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-hover" id="recentOrdersTable" width="100%" cellspacing="0">
                    <thead class="thead-light">
                        <tr>
                            <th>Order ID</th>
                            <th>Company</th>
                            <th>Transporter</th>
                            <th>Destination</th>
                            <th>Loading Date</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.RecentOrders.Any())
                        {
                            foreach (var order in Model.RecentOrders)
                            {
                                <tr>
                                    <td>#@order.Id</td>
                                    <td>@order.CompanyName</td>
                                    <td>@order.TransporterName</td>
                                    <td>@order.DestinationCity</td>
                                    <td>@order.DateForLoadingFrom.ToString("dd/MM/yyyy")</td>
                                    <td>
                                        <span class="badge badge-@GetStatusBadgeClass(order.Status)">
                                            @order.Status.ToString()
                                        </span>
                                    </td>
                                    <td>
                                    <td>
                                        <div class="d-flex flex-column" style="min-width: 90px;">
                                            <a asp-action="Details" asp-route-id="@order.Id"
                                               class="btn btn-info btn-sm mb-1 text-left"
                                               title="View Details">
                                                <i class="fas fa-eye mr-1"></i>Details
                                            </a>
                                            <a asp-action="Edit" asp-route-id="@order.Id"
                                               class="btn btn-warning btn-sm mb-1 text-left"
                                               title="Edit">
                                                <i class="fas fa-edit mr-1"></i>Edit
                                            </a>
                                            @if (order.Status == OrderStatus.Pending || order.Status == OrderStatus.Approved)
                                            {
                                                <button type="button" class="btn btn-danger btn-sm text-left"
                                                        title="Cancel" onclick="showCancelModal(@order.Id)">
                                                    <i class="fas fa-times mr-1"></i>Cancel
                                                </button>
                                            }
                                        </div>
                                    </td>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="7" class="text-center">No recent orders found</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Quick Action Cards -->
    <div class="row">
        <!-- Create Order Card -->
        <div class="col-lg-4 mb-4">
            <div class="card border-left-success shadow h-100">
                <div class="card-header bg-success text-white">
                    <h6 class="m-0 font-weight-bold">
                        <i class="fas fa-plus-circle mr-2"></i>Create New Order
                    </h6>
                </div>
                <div class="card-body">
                    <p>Create a new transportation order with all necessary details.</p>
                    <div class="text-center mt-4">
                        <a asp-action="Create" class="btn btn-success btn-lg">
                            <i class="fas fa-plus mr-2"></i>Create Order
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Manage Orders Card -->
        <div class="col-lg-4 mb-4">
            <div class="card border-left-primary shadow h-100">
                <div class="card-header bg-primary text-white">
                    <h6 class="m-0 font-weight-bold">
                        <i class="fas fa-tasks mr-2"></i>Manage Orders
                    </h6>
                </div>
                <div class="card-body">
                    <p>View, filter, and manage all existing orders in the system.</p>
                    <div class="text-center mt-4">
                        <a asp-action="ManageOrder" class="btn btn-primary btn-lg">
                            <i class="fas fa-list mr-2"></i>View All Orders
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Reports Card -->
        <div class="col-lg-4 mb-4">
            <div class="card border-left-info shadow h-100">
                <div class="card-header bg-info text-white">
                    <h6 class="m-0 font-weight-bold">
                        <i class="fas fa-chart-bar mr-2"></i>Order Reports
                    </h6>
                </div>
                <div class="card-body">
                    <p>Generate reports and analytics for orders and shipments.</p>
                    <div class="text-center mt-4">
                        <a asp-controller="Report" asp-action="Index" class="btn btn-info btn-lg">
                            <i class="fas fa-chart-pie mr-2"></i>View Reports
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Cancel Order Modal -->
<div class="modal fade" id="cancelOrderModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title">Cancel Order</h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span class="text-white">&times;</span>
                </button>
            </div>
            <form asp-action="Cancel" method="post">
                <div class="modal-body">
                    <input type="hidden" id="cancelOrderId" name="id" />
                    <div class="form-group">
                        <label for="cancelReason">Reason for Cancellation:</label>
                        <textarea class="form-control" id="cancelReason" name="reason"
                                  rows="3" required placeholder="Enter reason for cancellation..."></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-danger">Confirm Cancel</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Search Modal -->
<div class="modal fade" id="searchModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title">Search Orders</h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span class="text-white">&times;</span>
                </button>
            </div>
            <form asp-action="ManageOrder" method="get">
                <div class="modal-body">
                    <div class="form-group">
                        <label for="searchStatus">Status:</label>
                        <select class="form-control" id="searchStatus" name="status">
                            <option value="">All Statuses</option>
                            <option value="Pending">Pending</option>
                            <option value="In Progress">Approved</option>
                            <option value="Completed">Finished</option>
                            <option value="Cancelled">Cancelled</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="searchCompany">Company:</label>
                        <input type="text" class="form-control" id="searchCompany" name="company"
                               placeholder="Company name...">
                    </div>
                    <div class="form-group">
                        <label for="searchDateFrom">Date From:</label>
                        <input type="date" class="form-control" id="searchDateFrom" name="dateFrom">
                    </div>
                    <div class="form-group">
                        <label for="searchDateTo">Date To:</label>
                        <input type="date" class="form-control" id="searchDateTo" name="dateTo">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Search</button>
                </div>
            </form>
        </div>
    </div>
</div>
<script>
    function showCancelModal(orderId) {
        $('#cancelOrderId').val(orderId);
        $('#cancelReason').val('');
        $('#cancelOrderModal').modal('show');
    }

    // Initialize DataTable for recent orders
    $(document).ready(function() {
        $('#recentOrdersTable').DataTable({
            pageLength: 5,
            lengthMenu: [[5, 10, 25, 50, -1], [5, 10, 25, 50, "All"]],
            order: [[0, 'desc']]
        });
    });
</script>

@functions {
    public string GetStatusBadgeClass(OrderStatus status)
    {
        switch (status)
        {
            case OrderStatus.Pending:
                return "warning";
            case OrderStatus.Approved:
                return "info";
            case OrderStatus.Finished:
                return "success";
            case OrderStatus.Cancelled:
                return "danger";
            default:
                return "secondary";
        }
    }
}
<style>
    .action-buttons .btn {
        padding: 0.25rem 0.5rem;
        font-size: 0.8rem;
    }

        .action-buttons .btn i {
            margin-right: 3px;
        }
</style>